export const selectStudents = (state) => state.student.students;
export const selectTotalStudents = (state) => state.student.students.length;
export const selectLoadingStudents = (state) => state.exercice.loadingStudents;
