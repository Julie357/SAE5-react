export const selectClasses = (state) => state.clazz.classes;
export const selectTotalClasses = (state) => state.clazz.classes.length;
export const selectLoadingClasses = (state) => state.clazz.loadingClasses;
