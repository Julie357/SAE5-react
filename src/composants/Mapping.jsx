// import React from 'react';

// const TextVisualizer = ({ text, errors }) => {
//   const generateVisualText = () => {
//     const words = text.split(' ');

//     const visualText = words.map((word, index) => {
//       const error = errors[index] && errors[index].error;
//       return <span style={{ color: error ? 'red' : 'black' }}>{word} </span>;
//     });

//     return visualText;
//   };

//   return (
//     <div>
//       {generateVisualText()}
//     </div>
//   );
// };

// export default TextVisualizer;
